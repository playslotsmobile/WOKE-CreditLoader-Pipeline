{
  "name": "wokeavr-creditloader-pipeline",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
