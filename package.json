{
  "name": "wokeavr-creditloader-pipeline",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npx vite build",
    "start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
